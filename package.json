{
  "name": "ojax",
  "version": "1.0.0",
  "private": true,
  "description": "OjaX — the trusted campus marketplace & events hub for Nigerian university students",
  "license": "MIT",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n api,web -c yellow,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "db:reset": "npm --prefix server run db:reset",
    "db:purge-demo": "npm --prefix server run db:purge-demo",
    "test": "npm --prefix server run test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
